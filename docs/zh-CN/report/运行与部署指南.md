# 运行与部署指南

## 适用范围

- Docker 部署
- Docker Compose 部署
- Linux 二进制部署
- 反向代理场景

## 默认端口

- `codexmanager-service`：`48760`
- `codexmanager-web`：`48761`

## Docker Compose 部署

```bash
# 下载 release compose 文件
curl -L -o docker-compose.release.yml \
  https://github.com/chengliang4810/Codex-Manager-Server/releases/latest/download/docker-compose.release.yml

# 指定要部署的 release tag，例如 v0.2.5
CODEXMANAGER_RELEASE_TAG=v0.2.5 \
docker compose -f docker-compose.release.yml up -d
```

访问地址：

- `http://localhost:48761/`

## Docker 直接部署

```bash
# 共享数据卷
docker volume create codexmanager-data

# 容器网络
docker network create codexmanager-net

# service
docker run -d \
  --name codexmanager-service \
  --network codexmanager-net \
  --network-alias codexmanager-service \
  -p 48760:48760 \
  -v codexmanager-data:/data \
  -e CODEXMANAGER_SERVICE_ADDR=0.0.0.0:48760 \
  -e CODEXMANAGER_DB_PATH=/data/codexmanager.db \
  -e CODEXMANAGER_RPC_TOKEN_FILE=/data/codexmanager.rpc-token \
  ghcr.io/chengliang4810/codexmanager-service:latest

# web
docker run -d \
  --name codexmanager-web \
  --network codexmanager-net \
  -p 48761:48761 \
  -v codexmanager-data:/data \
  -e CODEXMANAGER_WEB_ADDR=0.0.0.0:48761 \
  -e CODEXMANAGER_WEB_NO_SPAWN_SERVICE=1 \
  -e CODEXMANAGER_WEB_NO_OPEN=1 \
  -e CODEXMANAGER_SERVICE_ADDR=codexmanager-service:48760 \
  -e CODEXMANAGER_DB_PATH=/data/codexmanager.db \
  -e CODEXMANAGER_RPC_TOKEN_FILE=/data/codexmanager.rpc-token \
  ghcr.io/chengliang4810/codexmanager-web:latest
```

## Linux 二进制部署

```bash
# 解压 Release 附件后
chmod +x codexmanager-service codexmanager-web codexmanager-start

# 先启动 service
./codexmanager-service

# 再启动 web
CODEXMANAGER_WEB_NO_SPAWN_SERVICE=1 \
CODEXMANAGER_SERVICE_ADDR=127.0.0.1:48760 \
./codexmanager-web
```

也可以用：

```bash
./codexmanager-start
```

## 反向代理要求

如果要把 `codexmanager-web` 放在 Nginx / Caddy 后面，至少要转发：

- `/api/runtime`
- `/api/rpc`
- 静态页面路由

只托管静态文件会导致管理页面无法完成 RPC 调用。

## 常见问题

### 页面能打开，但管理操作失败

优先检查：

1. `codexmanager-service` 是否健康
2. `codexmanager-web` 是否能访问 `codexmanager-service:48760`
3. 反向代理是否放通 `/api/runtime` 与 `/api/rpc`

### Docker 里 web 连不上 service

优先检查：

1. 两个容器是否在同一 Docker network
2. `CODEXMANAGER_SERVICE_ADDR` 是否写成了容器服务名
3. `codexmanager-web` 是否错误地去连宿主机端口
